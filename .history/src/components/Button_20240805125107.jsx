import React from "react";

const Button = ({ children, type = "button", bgColor = "bg-blue-500",
  textcolor='text-whitr'
 }) => {
  return <button className=""></button>;
};

export default Button;
